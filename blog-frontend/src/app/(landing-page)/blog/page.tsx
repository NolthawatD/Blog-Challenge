"use client";

import SearchBar from "@/components/searchBar";

export default function Home() {
	return (
		<div>
			<SearchBar handleToggleRemove={() => null} handleToggleEdit={() => null} />
		</div>
	);
}
